{
  "name": "app-exampapp-keyboard-nodele",
  "version": "1.0.2",
  "description": "App repository as an example",
  "main": "src/index.js",
  "engines": {
    "node": "13.6.0"
  },
  "scripts": {
    "start": "node src/index.js",
    "dev": "env $(cat .env | grep -v '#' | xargs) npm start",
    "rebuild": "rm -rf node_modules/ package-lock.json && npm i ",
    "test": "exit 0;"
  },
  "dependencies": {
    "input-event": "^1.2.6",
    "keypress": "~0.2.1"
  }
}
